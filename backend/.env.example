# ============================================================================
# FinMentor AI - Environment Variables Configuration
# ============================================================================
# SECURITY WARNING: Never commit your actual .env file to version control!
# Copy this file to .env and fill in your actual values
# ============================================================================

# ============================================================================
# REQUIRED SECURITY VARIABLES (Must be set before running)
# ============================================================================

# JWT Secret Key - CRITICAL for authentication security
# Generate using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# NEVER use a weak value - minimum 32 characters
JWT_SECRET_KEY=your-super-secret-jwt-key-min-32-chars-CHANGE-THIS

# Database Connection - PostgreSQL with credentials
# Format: postgresql://username:password@host:port/database_name
# Use a STRONG password, not "password"!
DATABASE_URL=postgresql://postgres:your_strong_password_here@localhost:5432/finmentor

# ============================================================================
# AI API KEYS - Understanding Embeddings vs LLM
# ============================================================================

# ============================================================================
# IMPORTANT: Two Separate AI Systems Work Together!
# ============================================================================
# 
# 1. EMBEDDINGS (for RAG/Semantic Search):
#    Purpose: Converts questions to vectors to find relevant information
#    Options:
#      a) Gemini embeddings (if GEMINI_API_KEY set) - Good quality, has quotas
#      b) OpenAI embeddings (if OPENAI_API_KEY set) - Excellent, very cheap
#      c) Local SentenceTransformer (if NO keys set) - FREE & UNLIMITED!
#
# 2. LLM (for Intelligent Responses):
#    Purpose: Generates actual answers to user questions
#    Options: Gemini-pro, GPT-4, Claude (requires at least ONE API key)
#
# RECOMMENDED CONFIGURATIONS:
# 
#   Development/Testing:
#     - Comment out ALL embedding keys → Uses FREE local embeddings
#     - Keep ONE LLM key active → Uses that for answers
#     Result: Unlimited searches + AI responses
#
#   Production:
#     - Use Gemini or OpenAI for embeddings → Better quality
#     - Use same or different LLM → Your choice
#     Result: Professional quality search + answers
# ============================================================================

# EMBEDDING_API_KEY - OPTIONAL: Dedicated embeddings provider
# Leave EMPTY for FREE local embeddings (unlimited, no quota!)
# Set to OpenAI key (sk-...) for cheap cloud embeddings ($0.13 per 1M queries)
# Set to Gemini key (AIza...) for high-quality embeddings (1,500/day free tier)
EMBEDDING_API_KEY=

# Google Gemini API (For LLM responses)
# Get from: https://makersuite.google.com/app/apikey
# Free tier: 60 requests/minute for LLM responses
GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI API (Alternative for LLM responses)
# Get from: https://platform.openai.com/api-keys
# Cost: $1-10 per 1M tokens (LLM), can also use for embeddings
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic Claude API (LLM responses only - no embeddings)
# Get from: https://console.anthropic.com/
# Note: Claude CANNOT be used for embeddings
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ============================================================================
# QUICK START TIP:
# For unlimited free testing:
#   EMBEDDING_API_KEY = (leave empty) → FREE local embeddings, unlimited
#   GEMINI_API_KEY = (your key) → Gemini LLM for answers
# ============================================================================

# ============================================================================
# CORS SECURITY (Production Required)
# ============================================================================

# Allowed Origins - CRITICAL for production security
# Development: http://localhost:3000,http://localhost:5173
# Production: https://yourdomain.com,https://app.yourdomain.com
# NEVER use "*" in production!
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================================
# AI MODEL CONFIGURATION (Optional - Has Defaults)
# ============================================================================

# Default LLM Model to use
# Options: gemini-pro, gemini-1.5-flash, gpt-3.5-turbo, gpt-4, claude-3-sonnet
DEFAULT_MODEL=gemini-pro

# Temperature (0.0-1.0) - Controls creativity vs determinism
# 0.0 = Very focused, deterministic
# 0.7 = Balanced (recommended)
# 1.0 = Very creative, varied
DEFAULT_TEMPERATURE=0.7

# Maximum tokens per response
DEFAULT_MAX_TOKENS=1000

# Verbose logging for debugging
VERBOSE_LOGGING=false

# ============================================================================
# SEARCH APIs (Optional - Free alternatives available)
# ============================================================================

# Google Custom Search (Optional)
GOOGLE_API_KEY=your-google-api-key-here
GOOGLE_CSE_ID=your-custom-search-engine-id-here

# Alternative Search APIs (All Optional)
SERPAPI_API_KEY=your-serpapi-key-here
BING_SUBSCRIPTION_KEY=your-bing-key-here

# Note: DuckDuckGo search works without API key (free fallback)

# ============================================================================
# FINANCIAL DATA APIs (Optional - Free alternatives available)
# ============================================================================

# Alpha Vantage - Stock data (Free tier: 25 requests/day)
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key-here

# News API - Financial news (Free tier: 100 requests/day)
NEWS_API_KEY=your-newsapi-key-here

# Financial Modeling Prep (Optional)
FMP_API_KEY=your-fmp-key-here

# Note: Yahoo Finance (yfinance) works without API key (free)

# ============================================================================
# OPTIONAL SERVICES
# ============================================================================

# Redis Cache (Optional - for better performance)
REDIS_URL=redis://localhost:6379

# ============================================================================
# ENVIRONMENT & DEBUGGING
# ============================================================================

# Environment: development, staging, production
ENVIRONMENT=development

# Debug mode - Shows detailed SQL queries and logs
# Set to False in production!
DEBUG=False

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# AUTHENTICATION CONFIGURATION (Optional - Has Defaults)
# ============================================================================

# Access token expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Refresh token expiration (days)
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================================================
# API RATE LIMITS (Optional - Has Defaults)
# ============================================================================

# Maximum requests per minute per user
MAX_REQUESTS_PER_MINUTE=60

# Maximum tokens per request
MAX_TOKENS_PER_REQUEST=2000

# ============================================================================
# QUICK START CHECKLIST
# ============================================================================
# 1. [ ] Copy this file to .env
# 2. [ ] Generate JWT_SECRET_KEY (use command above)
# 3. [ ] Set strong DATABASE_URL password
# 4. [ ] Add at least one AI API key (GEMINI_API_KEY recommended)
# 5. [ ] Configure ALLOWED_ORIGINS for your frontend
# 6. [ ] Never commit .env to git (check .gitignore)
# 7. [ ] Use different values for dev/staging/production
# ============================================================================

# ============================================================================
# FREE SERVICES (No API Keys Required)
# ============================================================================
# These work out of the box:
# - Yahoo Finance (yfinance) - Real-time stock data
# - DuckDuckGo Search - Web search
# - Federal Reserve Economic Data (FRED) - Economic indicators
# ============================================================================